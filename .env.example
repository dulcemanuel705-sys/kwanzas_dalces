# Database Configuration
DB_NAME=kwanza_manipulus
DB_USER=root
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_DIALECT=mysql
DB_PORT=3306

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-generate-new-one-256-bits-minimum

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration
CORS_ORIGIN=*

# Redis Configuration (for production)
REDIS_HOST=redis
REDIS_PORT=6379

# Logging Configuration
LOG_LEVEL=info

# File Upload Configuration
MAX_FILE_SIZE=25MB

# Security Configuration
BCRYPT_ROUNDS=12
